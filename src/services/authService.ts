// ============================================================================
// AUTH SERVICE
// Re-exports from API layer for backward compatibility
// ============================================================================

export { authApi, profileApi } from '@/api/auth';
export { apiClient, ApiError } from '@/api/client';
